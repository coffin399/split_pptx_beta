python-pptx>=0.6.21
# PySide6 provides the Qt based GUI (compatible with Python 3.7+)
# Install with:
#   pip install PySide6
# We target Python 3.10 in builds, but local dev can use 3.8-3.13
PySide6>=6.6.0,<6.8
Pillow>=10.0.0
pywin32>=305; platform_system == "Windows"
